#include <iostream>
#include <cmath>


using namespace std;

int main()
{

    /* Creating an array of named 'luckyNum[20]' that can store 20 items, although it
    can be an empty array now.
    Many times in programming work you may not know what are the value of the items
    that is inside of the array. */
    int luckyNums[20];

    // So can assign values to the item at index 0 straight away.
    luckyNums[0] = 100; // Assigning or changing the value of index 0.

    cout << luckyNums[0] << endl; // print out the value of index 0 from the array list of numbers.


    return 0;
}
